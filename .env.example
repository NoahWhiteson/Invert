# Economy API uses the same origin as this URL (wss→https). Omit = localhost:8787 (local D1, not Cloudflare Studio).
VITE_MULTIPLAYER_URL=wss://YOUR-WORKER-SUBDOMAIN.workers.dev
VITE_PASSWORD=temporary-password
